<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ccgr12024b_wbrj"
        tools:targetApi="31">

        <!-- Agrega esta línea con tu clave de API de Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCuuqRvWOEJREqffU5t6NMl-2gb57M4PVw"/>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Agregar Avión Activity (AGREGA ESTO) -->
        <activity
            android:name=".AgregarEstacionamientoActivity"
            android:exported="false" />

        <activity android:name=".MapaActivity" />
        <activity android:name=".DetalleEstacionamientoActivity"
            android:exported="false"/>

        <activity android:name=".EstacionamientosActivity" />
        <activity android:name=".SolicitudesActivity" />

    </application>

</manifest>
